# v0.5.1 验收记录 — VarStore 占位符与用户消息组合

> **对照计划**：[progress-v0.5.1.md](../log/progress-v0.5.1.md)  
> **验收日期**：（待填）  
> **范围**：`{{name}}` 与 VarStore、`UserMessageBuilder`、Runner `WithVarStore`、每步刷新、Outcome 交还 VarStore

---

## 结论摘要（验收时填写）

| 维度 | 状态 |
|------|------|
| 无 `RuntimeRequest` 业务参数字段；恢复依赖 `WithVarStore` + 外部 Materialize | **待验收** |
| system / user 中 `{{...}}` 由 VarStore 赋值；未命中保留原文 | **待验收** |
| 每步 LLM 前刷新 system 与首条 user 中的占位符 | **待验收** |
| 可选 `UserMessageBuilder`：先回调再替换；未设置则用 `UserMessage` | **待验收** |
| `RuntimeOutcome.VarStore` 交还、核心库不持久化 | **待验收** |
| `go test ./...` | **待验收** |

---

## 分步验收

### Step 1：Runner 与 VarStore

| 项 | 验收 |
|----|------|
| 仍仅通过 `runner.WithVarStore` 注入 Store，无新增请求侧参数 API | 待填 |
| Run 结束从 `QueryEnd`/`RuntimeOutcome` 可取回 `VarStore`；持久化在应用/SDK 层 | 待填 |

### Step 2：`{{...}}` 与 VarStore

| 项 | 验收 |
|----|------|
| `UserName` 与 store key `username` 命中；`user_name` 与 `UserName` 不混用 | 待填 |
| 未命中 key 时保留 `{{...}}` | 待填 |

### Step 3：UserMessageBuilder

| 项 | 验收 |
|----|------|
| 设置回调时：回调返回值经 VarStore 替换后作为 user 内容 | 待填 |
| 未设置回调：`UserMessage` 经 VarStore 替换 | 待填 |
| 回调返回 error 时 Run 报错终止（`user_message_builder`） | 待填 |
| Transfer 带 `inheritedMsgs` 时不调用回调 | 待填 |

### Step 4：刷新行为

| 项 | 验收 |
|----|------|
| 同一 Run 内 tool 修改 VarStore 后，下一步 LLM 的 system/user 占位符反映新值 | 待填 |

### Step 5：回归

| 项 | 验收 |
|----|------|
| `WithVariable` 时 `[Variables]` / `PromptBlock` 仍正确 | 待填 |

---

## 验收命令

在仓库 `loopForge` 目录下：

```bash
go test ./...
go build ./...
```

---

## TODO（发版前）

1. 验收通过后填写**验收日期**并更新结论摘要。
2. 若需字面量 `{{` 转义，列为后续版本或已知限制。
