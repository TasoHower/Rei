# v0.5.1 验收记录 — VarStore 占位符与 SystemPromptBuilder

> **对照计划**：[progress-v0.5.1.md](../log/progress-v0.5.1.md)  
> **验收日期**：2026-04-16  
> **范围**：`{{name}}` 与 VarStore、**SystemPromptBuilder**（原规划中的「用户侧 builder」口径已合并为 **system 侧动态组合**）、Runner `WithVarStore`、每步刷新、Outcome 交还 VarStore

---

## 结论摘要

| 维度 | 状态 | 说明 |
|------|------|------|
| 无 `RuntimeRequest` 业务参数字段；恢复依赖 `WithVarStore` + 外部 Materialize | **已满足** | `request.RuntimeRequest` 仍为 SessionID / UserMessage / RunMode / Options。 |
| system / user 中 `{{...}}` 由 VarStore 赋值；未命中保留原文 | **已满足** | `ReplaceDoubleBraceParams` + `brace_params_test.go`。 |
| 每步 LLM 前刷新 system 与首条 user 中的占位符 | **已满足** | `runLoopFullSystem` + `replaceSystemMessage`；每步 `replaceFirstUserMessage` + `ReplaceDoubleBraceParams(userTemplate, …)`。 |
| 可选 **SystemPromptBuilder**：每步合并后再 `{{}}`；未设置则默认合并 | **已满足** | `WithSystemPromptBuilder`、`runLoopFullSystem`；错误码 `system_prompt_builder`。 |
| user 侧：`UserMessage` 为模板 + `{{}}`（无单独 UserMessage builder） | **已满足** | 与当前代码一致。 |
| `RuntimeOutcome.VarStore` 交还、核心库不持久化 | **已满足** | Runner `WithVarStore`。 |
| `go test ./...`（`loopForge` 根目录） | **已满足** | 2026-04-16 自检通过。 |

**版本结论**：与 [progress-v0.5.1.md](../log/progress-v0.5.1.md) 交付清单一致，**v0.5.1 可视为本范围验收通过**。

---

## 分步验收

### Step 1：Runner 与 VarStore

| 项 | 验收 |
|----|------|
| 仍仅通过 `runner.WithVarStore` 注入 Store，无新增请求侧参数 API | **已满足** |
| Run 结束从 `QueryEnd`/`RuntimeOutcome` 可取回 `VarStore`；持久化在应用/SDK 层 | **已满足** |

### Step 2：`{{...}}` 与 VarStore

| 项 | 验收 |
|----|------|
| `UserName` 与 store key `username` 命中；`user_name` 与 `UserName` 不混用 | **已满足**（`brace_params_test.go`） |
| 未命中 key 时保留 `{{...}}` | **已满足** |

### Step 3：SystemPromptBuilder

| 项 | 验收 |
|----|------|
| 设置 `WithSystemPromptBuilder` 时：每步在 Variable 块合并后调用，再对结果做 VarStore `{{}}` 替换 | **已满足**（`runLoopFullSystem`、`user_message_test.go` 集成场景） |
| 未设置：默认 `SystemInstructions` + `PromptBlock()` 合并后再 `{{}}` | **已满足** |
| 回调返回 error 时 Run 终止，错误码 `system_prompt_builder` | **已满足**（`loop.go`） |
| Transfer 继承 `inheritedMsgs` 时：按当前 Agent 重算 system，不沿用「首轮 user 回调」叙事（user 仅为模板 + `{{}}`） | **已满足**（设计已与代码对齐） |

### Step 4：刷新行为

| 项 | 验收 |
|----|------|
| 同一 Run 内 tool 修改 VarStore 后，下一步 LLM 的 system/user 占位符反映新值 | **已满足** |

### Step 5：回归

| 项 | 验收 |
|----|------|
| `WithVariable` 时 `[Variables]` / `PromptBlock` 仍正确 | **已满足**（`TestSystemPromptBuilder_AssignParamAndAppendVarBlock` 等） |

---

## 验收命令

在仓库 `loopForge` 目录下：

```bash
go test ./...
go build ./...
```

**最近一次自检**（2026-04-16）：`go test ./...` 通过。

---

## 后续（可选）

1. 若需字面量 `{{` 转义，列为后续版本或已知限制。
