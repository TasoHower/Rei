# v0.5.2 验收记录 — Lark 适配器与 volcengine-go-sdk

> **对照计划**：[progress-v0.5.2.md](../log/progress-v0.5.2.md)  
> **验收日期**：2026-04-16  
> **范围**：`pkg/model/adapters/lark`（`volcengine-go-sdk` / arkruntime）、移除 `doubao`、环境与 demo 迁移、依赖与构建

---

## 结论摘要

| 维度 | 状态 | 说明 |
|------|------|------|
| `pkg/model/adapters/lark` 实现 `ToolCallingChatModel`（`Generate` + `Stream`，tools，流式含 usage） | **已满足** | `NewLarkChatModel`、`CreateChatCompletion` / `CreateChatCompletionStream`。 |
| 删除 `pkg/model/adapters/doubao`，全仓引用改为 `lark` | **已满足** | 无残留 `doubao` 包路径。 |
| 环境变量 **LARK_*** 为主，**DOUBAO_*** / **ARK_*** 兼容 | **已满足** | `agentdemo`、`doubaodemo`、`test-server`、`loopforged` 已对齐。 |
| `go.mod` 直接依赖 `github.com/volcengine/volcengine-go-sdk` | **已满足** | v1.2.24；`agent-sdk-go` 仍供 `agentsdk` 等模块使用。 |
| `go test ./...`、`go build ./...`（loopForge）；`test-server` 可构建 | **已满足** | 2026-04-16 验收时通过。 |

**版本结论**：与 [progress-v0.5.2.md](../log/progress-v0.5.2.md) 核心交付项一致，**v0.5.2 本范围验收通过**（针对 `lark` 包的独立 mock/录制测试列为可选后续，不阻塞本验收）。

---

## 分步验收

### Step 1：适配器与接口

| 项 | 验收 |
|----|------|
| `LarkChatModel.WithTools` 不可变绑定 tools | **已满足** |
| `CallOption`（temperature、max_tokens、top_p、model override）映射到 Ark 请求 | **已满足** |
| 流式 `StreamOptions.IncludeUsage` 用于 token 回填 | **已满足** |

### Step 2：仓库与入口

| 项 | 验收 |
|----|------|
| `cmd/agentdemo`、`test-server`、`cmd/doubaodemo`、`cmd/loopforged` 使用 `larkadapter.NewLarkChatModel` | **已满足** |
| `loopforged` 真实联调路径使用 loopForge lark + `Generate`（非旧 OpenAI Provider 直连） | **已满足** |

### Step 3：文档与根 README

| 项 | 验收 |
|----|------|
| 根目录 `README.md` 中 test-server / 技术栈描述与 Lark 一致 | **已满足** |
| `doc/log/progress-v0.5.2.md` 与交付清单一致 | **已满足** |

---

## 验收命令

在仓库 `loopForge` 目录：

```bash
go test ./...
go build ./...
```

在 `test-server` 目录：

```bash
go build .
```

**最近一次自检**（2026-04-16）：上述命令通过。

---

## v0.5.2 补丁记录（验收后）

| 日期 | 内容 |
|------|------|
| 2026-04-16 | Runner 默认 Lark、`ApplyLarkFromConfig`、test-server 对齐；Ark 工具 schema 与 `var_set` 修复豆包 400（详见 [progress-v0.5.2.md 变更日志](../log/progress-v0.5.2.md)）。 |

---

## 后续（非本版本必达）

1. 针对 `pkg/model/adapters/lark` 的 **HTTP mock 或 golden 录制**单测（便于 CI 无 Key 回归）。
2. 在 `doc/decision/sdk-selection.md` 中补充 **Lark / volcengine-go-sdk** 选型说明（可选）。
