# v0.5.0 验收记录 — Variable

> **对照计划**：[progress-v0.5.0.md](../log/progress-v0.5.0.md)  
> **验收日期**：2026-04-16  
> **范围**：当前工作区未提交变更（`pkg/variable`、`pkg/errors`、Agent/Runner/Outcome/Event、tool、agentsdk、test-server）

---

## 结论摘要

| 维度 | 状态 |
|------|------|
| 核心库 `pkg/variable`（Store、Manifest/Materialize、context、typed、`var_set`） | **已落地**，`go test ./...` 通过 |
| Agent `WithVariable`、RunLoop 注入 context、`PromptBlock` 拼接 | **已落地** |
| Runner `WithVarStore`、Transfer 链共享同一 Store、Outcome 带回 `VarStore` | **已落地** |
| `InterceptedCall.VarSnapshot`（transfer 拦截时快照） | **已落地** |
| `EventVarChange` / `VarChangePayload` 类型与 accessor | **已定义**；**运行时未发射事件**（见 TODO） |
| 计划中的 Agent/Runner 层集成测试 | **未单独补充**（见 TODO） |
| `cmd/agentdemo` Variable 演示 | **未做**（见 TODO） |
| `test-server` Materialize + 会话快照 + UI 展示变量 | **已做** |

---

## 分步对照（与计划交付清单）

### Step 1：`pkg/variable`

| 项 | 验收 |
|----|------|
| `store.go` VarStore / VarEntry / CRUD / Define / AgentSet / PromptBlock / Snapshot·Import / JSON | 已实现 |
| `const.go` | 已实现 |
| `context.go` | 已实现 |
| `typed.go` | 已实现 |
| `manifest.go` Materialize（含可选孤儿清理等） | 已实现 |

### Step 2：`var_set`

| 项 | 验收 |
|----|------|
| `tools.go` + `const_` 保护 | 已实现；与 `pkg/errors` 哨兵错误配合 |

### Step 3–6：Agent / Runner / Outcome

| 项 | 验收 |
|----|------|
| `WithVariable()`、`Agent.Variable`、`Clone` | 已实现 |
| `LoopState.VarStore`、RunLoop 注入、每步 LLM 前 `PromptBlock` | 已实现 |
| `WithVarStore`、`runTransferLoop` 使用同一 Store | 已实现 |
| `RuntimeOutcome.VarStore` | 已实现（JSON 标签为 `-`，与计划示例一致为引用传递） |
| `InterceptedCall.VarSnapshot` | 已实现 |

### Step 7：Event（可选）

| 项 | 验收 |
|----|------|
| `EventVarChange`、`VarChangePayload`、`RuntimeEvent.VarChange()` | **类型已就绪** |
| 在 Set / Delete / `var_set` 等路径上 `ch <-` 发射事件 | **未实现** |

### Step 8：测试

| 项 | 验收 |
|----|------|
| `pkg/variable/*_test.go` | 已存在并通过 |
| `pkg/agent` Variable + prompt 集成测试 | **缺失** |
| `pkg/runner` Transfer + `WithVarStore` + outcome | **缺失** |
| 全链路集成测试（外部写入 → Run → Snapshot → Import） | **缺失** |
| `go test ./...` | **通过**（验收时） |

### Step 9：文档与示例

| 项 | 验收 |
|----|------|
| `pkg/variable/doc.go` | 已添加 |
| `test-server` 演示 | 已增强（Manifest、Materialize、持久化、`query_end` 中带 snapshot） |
| `cmd/agentdemo` | **未**接入 Variable |

### 其他变更（计划外但同批代码）

| 项 | 说明 |
|----|------|
| `pkg/errors` | 集中哨兵错误，`tool` / `variable` 等引用 |
| `pkg/model/adapters/agentsdk` | 与错误类型/转换的小幅调整 |
| `pkg/tool/dispatch.go`、`map.go` | 使用统一错误类型 |

---

## TODO（发版或后续迭代前建议完成）

以下为对照计划仍**未闭合**或可改进项，按优先级大致排序：

1. **可观测性**：在变量写入/删除路径发射 `EventVarChange`（若产品需要 SSE/审计）；必要时在 RunLoop 或 `VarStore` 层增加回调，避免遗漏。
2. **测试**：补充 `pkg/agent` 侧「开启 `WithVariable` + `PromptBlock` 含 `[Variables]`」的集成测试；补充 `pkg/runner` 侧多 Agent Transfer 后 `Outcome.VarStore` 一致性与 `WithVarStore` 行为测试。
3. **示例**：在 `cmd/agentdemo` 中增加最小 Variable 示例（或文档说明以 test-server 为准）。
4. **端到端**：可选增加跨包集成测试：Materialize → Runner → `Snapshot` → `Import` 往返。
5. **计划文档同步**：将 [progress-v0.5.0.md](../log/progress-v0.5.0.md) 交付清单勾选与实际进度对齐（验收通过后由维护者更新）。

---

## 验收命令（可复现）

在仓库 `loopForge` 目录下执行：

```bash
go test ./...
go build ./...
```

（可选）编译 `test-server`：

```bash
go build -o /dev/null ./test-server/...
```

---

## 变更文件索引（便于评审）

未提交变更涉及（非穷举）：`pkg/variable/**`、`pkg/errors/**`、`pkg/agent/*.go`、`pkg/runner/runner.go`、`pkg/runtime/event/event.go`、`pkg/runtime/outcome/outcome.go`、`pkg/tool/dispatch.go`、`pkg/tool/map.go`、`pkg/model/adapters/agentsdk/*.go`、`test-server/main.go`、`test-server/static/index.html`、`doc/log/progress-v0.5.0.md` 等。
