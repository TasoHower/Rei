package defaults

// Observability and process identity.
const (
	ServiceName    = "loopforge"
	ServiceVersion = "0.1.0"
)

// BuiltinSpawnToolName is the default exposed name for the spawn builtin (wire-up may override).
const BuiltinSpawnToolName = "spawn_subagent"
