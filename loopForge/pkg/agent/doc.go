// Package agent defines the minimal runnable contracts from doc/design/abstractions.md section 2.4
// (Agent, Planner) mapped to loopForge runtime types. RunnerAgent is the default concrete Agent:
// a pkg/model chat loop with function calling; tool execution helpers live in loopforge/pkg/tool.
package agent
